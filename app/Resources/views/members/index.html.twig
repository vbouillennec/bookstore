{% extends 'base.html.twig' %}

{% block titre %}
    Liste membres
{% endblock %}


{% block body %}
    <br/>

    <table class="table">
        <thead>
            <th>Photo</th>
            <th>Prénom</th>
            <th>Nom</th>
            <th>Email</th>
            <th></th>
        </thead>
        <tbody>
            {% for id, member in members %}
                <tr>
                    <td><img src="{{ asset(member.photo) }}"/></td>
                    <td>{{ member.prenom }}</td>
                    <td>{{ member.nom }}</td>
                    <td>{{ member.email }}</td>
                    <td><a href="{{ url('member', {'id' : id })}}" class="btn btn-primary">Voir</a></td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}

{% block stylesheets %}
{% endblock %}